# Linear Search

Overview

Linear search checks each element one-by-one and runs in O(n) time.

Sentinel optimization
- Place the target at the end to avoid bounds checking inside loop (useful in low-level languages).
