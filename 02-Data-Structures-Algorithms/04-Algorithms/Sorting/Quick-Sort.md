# Quick Sort

Overview

QuickSort is a divide-and-conquer algorithm. Average-case O(n log n), worst-case O(n^2) without good pivot selection.

Partition schemes
- Lomuto, Hoare

Example notes
- Use randomized pivot or median-of-three to avoid worst-case on sorted inputs.

TODO: add partition code and iterative variants.
